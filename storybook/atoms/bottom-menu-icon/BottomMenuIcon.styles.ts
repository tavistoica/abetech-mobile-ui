import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  drawerItem: {
    flexDirection: "row",
    alignItems: "center",
    opacity: 1,
    width: "20%",
    height: "100%",
    justifyContent: "space-evenly",
  },
});
